---
phase: 04-nba-parlay-ai
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - cogs/parlay.py
autonomous: false
requirements:
  - PAR-06
  - PAR-07
  - PAR-08
  - PAR-14

must_haves:
  truths:
    - "Adding ✅ reaction to a parlay message marks it HIT and increases leg_type_weights"
    - "Adding ❌ reaction to a parlay message marks it MISS and decreases leg_type_weights"
    - "Bot's own reactions are completely ignored"
    - "Only the first valid ✅ or ❌ reaction per parlay is recorded (subsequent reactions ignored)"
    - "Reactions older than 24 hours are ignored"
    - "Weight changes persist after bot restart (stored in leg_type_weights table)"
    - "After 20+ tracked parlays, leg types with < 30% hit rate are filtered from generate_parlay()"
  artifacts:
    - path: "cogs/parlay.py"
      provides: "on_raw_reaction_add listener + weight update logic"
      contains: "on_raw_reaction_add"
  key_links:
    - from: "cogs/parlay.py on_raw_reaction_add"
      to: "parlays table"
      via: "SELECT_PARLAY_BY_MESSAGE_ID to look up parlay by Discord message_id"
      pattern: "SELECT_PARLAY_BY_MESSAGE_ID"
    - from: "cogs/parlay.py on_raw_reaction_add"
      to: "leg_type_weights table"
      via: "UPSERT_LEG_TYPE_WEIGHT_HIT / UPSERT_LEG_TYPE_WEIGHT_MISS per leg in parlay_legs"
      pattern: "UPSERT_LEG_TYPE_WEIGHT"
    - from: "cogs/parlay.py on_raw_reaction_add"
      to: "parlays.outcome"
      via: "UPDATE_PARLAY_OUTCOME — set to 'hit' or 'miss' after first valid reaction"
      pattern: "UPDATE_PARLAY_OUTCOME"
---

<objective>
Add the self-learning reaction handler to ParlayCog. This is the feedback loop that makes the
AI improve over time: ✅/❌ reactions on posted parlays update leg_type_weights in the database,
which affect future parlay generation scores.

Purpose: Without this plan, parlays generate but never improve. This closes the feedback loop
and makes the AI self-learning.

Output: on_raw_reaction_add listener added to cogs/parlay.py (file already exists from Plan 03)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nba-parlay-ai/04-RESEARCH.md
@.planning/phases/04-nba-parlay-ai/04-CONTEXT.md
@.planning/phases/04-nba-parlay-ai/04-03-SUMMARY.md

@cogs/parlay.py
@database/queries.py
@database/db.py
@config.py
</context>

<interfaces>
<!-- Key contracts extracted from codebase for executor. -->

From database/queries.py (Plan 01 output) — reaction handler SQL constants:
```python
SELECT_PARLAY_BY_MESSAGE_ID  # (discord_message_id,) → (id, generated_at, outcome)
SELECT_PARLAY_LEGS           # (parlay_id,) → [(leg_type, leg_score), ...]
UPDATE_PARLAY_OUTCOME        # (outcome, parlay_id) — set "hit" or "miss"
UPSERT_LEG_TYPE_WEIGHT_HIT   # (leg_type, new_weight) — ON CONFLICT increments hit_count
UPSERT_LEG_TYPE_WEIGHT_MISS  # (leg_type, new_weight) — ON CONFLICT increments miss_count
SELECT_LEG_TYPE_WEIGHT       # (leg_type,) → (weight, hit_count, miss_count)
```

From config.py:
```python
config.PARLAY_CHANNEL_ID: int
config.PARLAY_LEARNING_RATE: float  # default 0.05; delta = ±1
```

Reaction handler pattern from RESEARCH.md Pattern 2:
```python
@commands.Cog.listener()
async def on_raw_reaction_add(self, payload: discord.RawReactionActionEvent) -> None:
    # payload.member.bot       — True if reactor is a bot
    # payload.channel_id       — channel the reaction was added in
    # payload.emoji.name       — "✅" or "❌" (unicode) or "white_check_mark" / "x"
    # payload.message_id       — message that was reacted to
```

Emoji matching note (RESEARCH.md Pitfall 4):
  Check BOTH payload.emoji.name == "✅" AND payload.emoji.name == "white_check_mark"
  Check BOTH payload.emoji.name == "❌" AND payload.emoji.name == "x"

Weight update formula (PAR-07, locked):
  delta = +1 if HIT, -1 if MISS
  new_weight = old_weight + (config.PARLAY_LEARNING_RATE * delta)
  Weight floor: max(0.1, new_weight) — never let weight drop to 0 or negative

"First reaction wins" enforcement (Decision D, PAR-14):
  DB-authoritative: check parlays.outcome != "pending" before processing.
  This is restart-safe (in-memory set from Plan 3 research is NOT used here —
  instead query DB outcome to handle restarts correctly).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add on_raw_reaction_add to ParlayCog</name>
  <files>cogs/parlay.py</files>
  <action>
Add the on_raw_reaction_add listener method to the ParlayCog class in cogs/parlay.py.
Also add the missing SQL imports to the existing import block at the top of the file.

ADD TO IMPORTS (update the existing database.queries import line):
  Add: SELECT_PARLAY_BY_MESSAGE_ID, SELECT_PARLAY_LEGS, UPDATE_PARLAY_OUTCOME,
       SELECT_LEG_TYPE_WEIGHT, UPSERT_LEG_TYPE_WEIGHT_HIT, UPSERT_LEG_TYPE_WEIGHT_MISS

ADD TO IMPORTS (add to datetime import):
  from datetime import datetime, timedelta, timezone

ADD METHOD to ParlayCog class (before setup() at the bottom):

  @commands.Cog.listener()
  async def on_raw_reaction_add(self, payload: discord.RawReactionActionEvent) -> None:
    """Handle ✅/❌ reactions on posted parlay messages. PAR-06, PAR-14.

    Enforces: bot reactions ignored, 24h window, first-reaction-wins (DB authoritative),
    PARLAY_CHANNEL_ID scope only. Updates leg_type_weights per PAR-07.
    """
    # 1. Ignore bot reactions (PAR-14)
    if payload.member is None or payload.member.bot:
        return

    # 2. Only react to messages in PARLAY_CHANNEL_ID
    if payload.channel_id != config.PARLAY_CHANNEL_ID:
        return

    # 3. Only ✅ and ❌ emoji (handle both unicode and name variants, PAR-14 / Pitfall 4)
    emoji_name = payload.emoji.name
    is_hit = emoji_name in ("✅", "white_check_mark")
    is_miss = emoji_name in ("❌", "x")
    if not (is_hit or is_miss):
        return

    # 4. Look up parlay by message_id
    async with get_db() as db:
        cursor = await db.execute(SELECT_PARLAY_BY_MESSAGE_ID, (str(payload.message_id),))
        row = await cursor.fetchone()

    if not row:
        return  # Not a parlay message

    parlay_id = row["id"]
    generated_at_iso = row["generated_at"]
    current_outcome = row["outcome"]

    # 5. First-reaction-wins: skip if already scored (PAR-14, Decision D)
    # DB-authoritative — handles restarts correctly (no in-memory set needed)
    if current_outcome != "pending":
        return

    # 6. Enforce 24-hour window (PAR-14, Decision D)
    try:
        generated_at = datetime.fromisoformat(generated_at_iso)
        if generated_at.tzinfo is None:
            generated_at = generated_at.replace(tzinfo=timezone.utc)
        if datetime.now(tz=timezone.utc) - generated_at > timedelta(hours=24):
            return  # Reaction too late
    except (ValueError, TypeError):
        logger.warning(f"ParlayCog: could not parse generated_at for parlay {parlay_id}")
        return

    # 7. Determine outcome
    outcome = "hit" if is_hit else "miss"
    delta = 1 if is_hit else -1

    # 8. Update parlay outcome and leg_type_weights atomically (PAR-06, PAR-07)
    async with get_db() as db:
        # Mark parlay outcome first (prevents race condition on repeated rapid reactions)
        await db.execute(UPDATE_PARLAY_OUTCOME, (outcome, parlay_id))

        # Fetch legs for this parlay
        cursor = await db.execute(SELECT_PARLAY_LEGS, (parlay_id,))
        legs = await cursor.fetchall()

        for leg in legs:
            leg_type = leg["leg_type"]

            # Get current weight
            w_cursor = await db.execute(SELECT_LEG_TYPE_WEIGHT, (leg_type,))
            w_row = await w_cursor.fetchone()
            old_weight = w_row["weight"] if w_row else 1.0

            # Calculate new weight (PAR-07): new = old + (LEARNING_RATE * delta)
            # Floor at 0.1 so weights never go to zero or negative
            new_weight = max(0.1, old_weight + (config.PARLAY_LEARNING_RATE * delta))

            # Upsert weight + increment appropriate counter
            if is_hit:
                await db.execute(UPSERT_LEG_TYPE_WEIGHT_HIT, (leg_type, new_weight))
            else:
                await db.execute(UPSERT_LEG_TYPE_WEIGHT_MISS, (leg_type, new_weight))

    logger.info(
        f"ParlayCog: parlay {parlay_id} marked {outcome.upper()} "
        f"by {payload.member} — weights updated for {len(legs)} legs"
    )
  </action>
  <verify>
    <automated>cd /Users/ringochui/Projects/chewyBot && python -c "
import ast
with open('cogs/parlay.py') as f:
    src = f.read()
tree = ast.parse(src)
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.AsyncFunctionDef) or isinstance(n, ast.FunctionDef)]
print('functions:', funcs)
assert 'on_raw_reaction_add' in funcs, 'on_raw_reaction_add listener missing'
# Check key constants are imported
assert 'UPDATE_PARLAY_OUTCOME' in src, 'UPDATE_PARLAY_OUTCOME not imported'
assert 'UPSERT_LEG_TYPE_WEIGHT_HIT' in src, 'UPSERT_LEG_TYPE_WEIGHT_HIT not imported'
assert 'UPSERT_LEG_TYPE_WEIGHT_MISS' in src, 'UPSERT_LEG_TYPE_WEIGHT_MISS not imported'
assert 'white_check_mark' in src, 'emoji name variant not handled (Pitfall 4)'
assert 'outcome != .pending.' in src or 'current_outcome != \"pending\"' in src or \"current_outcome != 'pending'\" in src, 'first-reaction-wins check missing'
assert 'timedelta' in src, '24h window check missing'
assert '0.1' in src, 'weight floor missing'
print('All reaction handler checks passed')
"</automated>
  </verify>
  <done>on_raw_reaction_add is present in ParlayCog. It checks bot reactions, channel scope, emoji names (including unicode variants), 24h window, first-reaction-wins via DB outcome check, and updates leg_type_weights with new_weight = max(0.1, old + learning_rate * delta).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end NBA Parlay AI smoke test</name>
  <files>cogs/parlay.py, database/queries.py, adapters/balldontlie.py</files>
  <action>Human smoke test of the complete NBA Parlay AI feature end-to-end (see how-to-verify steps).</action>
  <verify>Human confirms parlay posts, reactions update weights, weights persist after restart.</verify>
  <done>All 9 verification steps pass: bot loads, /parlay posts embed, ✅ reaction updates weights, /parlay_stats shows data, weights survive restart.</done>
  <what-built>
    Complete NBA Parlay AI cog (Plans 01-04): BallDontLie adapter, parlay engine with
    5-factor scoring, ParlayCog with daily post, 3 slash commands, and reaction learning system.
  </what-built>
  <how-to-verify>
    1. Start bot with MOCK_MODE=true: `python bot.py`
    2. Confirm "chewyBot has logged in!" appears in LOG_CHANNEL_ID and no errors in chewybot.log
    3. Run /parlay in Discord — confirm parlay embed appears in PARLAY_CHANNEL_ID with:
       - Title: "chewyBot's NBA Parlay — [date]"
       - 3-5 legs showing team, market type, American odds
       - Combined parlay odds field
       - Confidence score field (0-100)
       - Footer with reaction prompt
    4. React ✅ to the parlay message — confirm no error in logs
    5. Run /parlay_stats — confirm it shows total tracked, hit rate, and leg type breakdown
    6. Run /parlay_history 3 — confirm it shows recent parlays with outcomes
    7. Run /parlay again — confirm the parlay that was reacted to shows as "hit" in history
    8. Check SQLite DB: `sqlite3 chewybot.db "SELECT * FROM leg_type_weights;"` — confirm weights changed from 1.0
    9. Restart bot — re-run /parlay_stats and confirm weights are still updated (persisted)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. AST check confirms on_raw_reaction_add present in ParlayCog
2. Import check: UPDATE_PARLAY_OUTCOME, UPSERT_LEG_TYPE_WEIGHT_HIT, UPSERT_LEG_TYPE_WEIGHT_MISS all imported
3. Emoji variant handling: both "✅"/"white_check_mark" and "❌"/"x" covered
4. First-reaction-wins: DB outcome check for "pending" present
5. 24h window: timedelta(hours=24) comparison present
6. Weight floor: max(0.1, ...) present
7. Full bot smoke test via human verification checkpoint
</verification>

<success_criteria>
- ✅ reaction on posted parlay → outcome = "hit" in parlays table, leg_type_weights weight increases
- ❌ reaction on posted parlay → outcome = "miss" in parlays table, leg_type_weights weight decreases
- Second reaction on same parlay → ignored (DB outcome != "pending")
- Bot reaction → ignored (payload.member.bot check)
- Reaction after 24h → ignored (timedelta check)
- Weight changes survive bot restart (DB-persisted, not in-memory)
- All 14 PAR requirements addressed across Plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/04-nba-parlay-ai/04-04-SUMMARY.md`
</output>
